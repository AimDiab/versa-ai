# Base URL of the FastAPI backend (server-side rewrite target, dev and prod).
# Example: API_URL=https://api.yourdomain.com
API_URL=http://localhost:8000

# Exposed to the browser — set in production if calling the API directly
# (bypassing the Next.js rewrite). Leave empty to use the rewrite.
NEXT_PUBLIC_API_URL=
